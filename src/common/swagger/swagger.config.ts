import { SWAGGER_CONFIG_TYPES } from './swagger.interface';

export const SWAGGER_CONFIG: SWAGGER_CONFIG_TYPES = {
  title: 'QUANT READER API GUIDE',
  description: 'Learn about API Uses',
  version: '1.0',
};
